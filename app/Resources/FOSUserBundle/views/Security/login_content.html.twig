{% trans_default_domain 'FOSUserBundle' %}
<div class="page-content container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="login-wrapper">
                    <div class="box">
                        <div class="content-wrap">
<form action="{{ path("fos_user_security_check") }}" method="post">
    {% if csrf_token %}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
    {% endif %}
                            <h6>Sign In</h6>
                            {% if error %}
            <span class="help-block">{{ error.messageKey|trans(error.messageData, 'security') }}</span>
            {% endif %}
                            <input id="username" name="_username" class="form-control" type="text" placeholder="{{ 'security.login.username'|trans }}">
                            <input id="password" name="_password" class="form-control" type="password" placeholder="{{ 'security.login.password'|trans }}">
                            <input id="_submit" name="_submit"  class="form-control btn btn-primary signup" type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
                            <input class="" id="remember_me" name="_remember_me" type="checkbox" value="on"> Recordar
                                            
</form><div class="action">
        <a href="{{ path("fos_user_resetting_request") }}">Olvide mi contraseña</a><br>
    </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
